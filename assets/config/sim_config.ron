// Every simulation coefficient (GDD §5.9), hot-reloadable while `cargo run`
// is active (task 073) — save this file and the running game picks up the
// new values without restarting. Values here must match `SimConfig::default()`
// in `src/config.rs`; that hand-written `Default` impl exists so tests don't
// need Bevy's asset machinery, and the two are kept in sync by hand.
(
    grid: (
        width: 128,
        height: 80,
        neighborhood_size: 8,
    ),
    camera: (
        zoom_min: 0.1,
        zoom_max: 1.0,
        zoom_threshold: 0.4,
        zoom_speed: 0.9,
        pan_speed: 120.0,
    ),
    cluster: (
        density_saturation: 20.0,
    ),
    environment: (
        diffusion_rate: 0.05,
        light_high: 0.9,
        light_low: 0.2,
        source_temperature: 0.85,
        ambient_temperature: 0.25,
        toxic_zone_value: 0.7,
        toxic_zone_width: 21,
        toxic_zone_height: 15,
        stress_delta: 0.3,
    ),
    time: (
        era_ticks: 25,
        era_budget_early: 60,
        era_budget_late: 45,
        point_budget_per_era: 3,
        action_costs: (
            seed: 1,
            stress: 1,
            cull: 1,
            splice: 2,
        ),
        era_tick_hz: 8.0,
        grace_eras: 3,
        onboarding_eras: 3,
        onboarding_era_ticks: 8,
    ),
    energy: (
        seed_energy: 5.0,
        base_upkeep: 0.5,
        crowd_factor: 0.15,
        repro_threshold: 10.0,
        repro_cost: 5.0,
        photolithic_metabolism_gain: 2.0,
        predator_drain_cap: 2.0,
        predator_upkeep: 0.7,
        decomposer_extract_rate: 1.5,
        decomposer_upkeep: 0.5,
        residue_on_death: 3.0,
        residue_decay: 0.2,
        residue_ambient_trickle: 0.05,
        default_temp_tolerance: 0.15,
        splice_temp_shift: 0.15,
        trend_epsilon: 0.5,
    ),
    tags: (
        global_tag_pool: 10,
        active_tags_early: 5,
        active_tags_late: 8,
        tags_per_species_min: 1,
        tags_per_species_max: 3,
        effect_intensity_min: -2,
        effect_intensity_max: 2,
        matrix_density: 0.4,
    ),
    notebook: (
        confirmation_threshold: 3.0,
        observation_weight_numerator: 1.0,
    ),
    difficulty: (
        ramp_worlds: 3,
        toxic_zone_width_late: 43,
        toxic_zone_height_late: 30,
        matrix_density_late: 0.6,
        objective_severity_early: 1.0,
        objective_severity_late: 2.0,
        objective_count_early: 2,
        objective_count_late: 3,
    ),
    worldgen: (
        starting_species_count: 2,
        extra_available_species_count: 1,
    ),
    objectives: (
        coexistence_min_species_base: 3,
        coexistence_ticks_base: 100,
        survive_in_ticks_base: 75,
        trigger_bloom_population_threshold_base: 8,
    ),
    terrain: (
        continent_wave_count: 3,
        continent_freq_min: 0.8,
        continent_freq_max: 1.6,
        island_wave_count: 6,
        island_freq_min: 12.0,
        island_freq_max: 18.0,
        island_blend_weight: 0.45,
        sea_threshold: 0.42,
        hill_threshold: 0.62,
        mountain_threshold: 0.8,
        peak_elevation_threshold: 0.88,
        min_placeable_fraction: 0.4,
        max_generation_attempts: 8,
        min_toxic_zone_placeable_fraction: 0.5,
        max_toxic_zone_placement_attempts: 24,
    ),
    source: (
        heat_source_count_early: 2,
        heat_source_count_late: 4,
        heat_source_radius_early: 40.0,
        heat_source_radius_late: 25.0,
        wind_strength_early: 5.0,
        wind_strength_late: 15.0,
        heat_source_min_distance: 20.0,
        max_heat_source_placement_attempts: 24,
        reinjection_strength: 0.15,
        sea_coolant_strength: 0.05,
        sea_coolant_value: 0.1,
        mountain_shade_radius: 8.0,
        mountain_shade_strength: 0.3,
    ),
)
